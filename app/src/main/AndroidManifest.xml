<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chinesemobile.chinesemobile">
    <!-- All require permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChineseMobile">
        <activity android:name=".HomeUserActivity"></activity>
        <activity android:name=".activities.VocabularyListUserActivity" />
        <activity android:name=".VocabularyDetailActivity" />
        <activity android:name=".activities.VocabEditActivity" />
        <activity android:name=".activities.VocabListAdminActivity" />
        <activity android:name=".activities.VocabAddActivity" />
        <activity android:name=".activities.VocabularyCategoryAddActivity" />
        <activity android:name=".activities.DashboardAdminActivity" />
        <activity android:name=".activities.DashboardUserActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>